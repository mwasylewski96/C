#include <stdlib.h>
/*
#define SIZE 8
#define AMOUNTOFPOSSIBLEELEMENTS 6
#define MAPVAL 5
void main()
{
    int arr[SIZE] = {5,7,7,7,7,7,10,10};
    int countArr[AMOUNTOFPOSSIBLEELEMENTS] = {0};

    for (int i=0; i<SIZE; i++)
    {
        countArr[arr[i]-MAPVAL]++;
    }

    for (int i=0; i<SIZE; i++)
    {
        printf("%d ", arr[i]);
    }

    printf("\n");

    for (int i=0; i<AMOUNTOFPOSSIBLEELEMENTS; i++)
    {
        printf("%d ", countArr[i]);
    }
    int max = 0;
    int amount = 0;
    for (int i=0;i<AMOUNTOFPOSSIBLEELEMENTS;i++)
    {
        if (countArr[i] > amount)
        {
            max = i;
            amount = countArr[i];
        }
    }
    printf("\nValue %d appears most time: %d", max+MAPVAL, amount);

}
*/
