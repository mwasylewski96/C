#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#define NUMBER_OF_ELEMENTS 10
/*
void collect_all_numbers_to_array(int tab[])
{
    printf("Insert all values to array:\n");
    for (int i=0; i<NUMBER_OF_ELEMENTS; i++)
    {
        scanf("%d", &tab[i]);
    }
}

bool check_function_has_good_neighbours(int tab[])
{
    bool has_good_neigbours = false;
    int i;
    for (i=1; i<NUMBER_OF_ELEMENTS-1; i++)
    {
        if (tab[i-1]*tab[i+1]==tab[i])
            {
                has_good_neigbours = true;
                printf("%d * %d = %d\n", tab[i-1], tab[i+1], tab[i]);
                return has_good_neigbours;
            }

    }
    return has_good_neigbours;
}

void main()
{
    int tablica[NUMBER_OF_ELEMENTS];
    collect_all_numbers_to_array(tablica);
    bool value_of_bool = check_function_has_good_neighbours(tablica);
    if (value_of_bool==1)
        printf("The array has good neighbours");
    else
        printf("The array has no good neighbours");
}
*/
