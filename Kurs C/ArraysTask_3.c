#include <stdlib.h>
#define SIZE 8
/*
void main()
{
    int arr_1[] = {1,3,7,4,2,2,1,3};
    int max_val = arr_1[0] + arr_1[1];
    for (int i=1; i<SIZE; i++)
        if (arr_1[i-1]+arr_1[i]>= max_val)
            max_val = arr_1[i-1]+arr_1[i];
    printf("Largest sum is %d", max_val);
}
*/
