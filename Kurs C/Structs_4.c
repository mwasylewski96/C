#include <stdlib.h>
#include <stdio.h>
/*
typedef struct point{
    int x;
    int y;
}POINT;

POINT insertPointCoordinates();
void initializeCoordinatesOfPointsArray(POINT *points);
void printCoordinatesOfAllPointsInArrays(POINT *points);


void main()
{
    POINT points[5];
    initializeCoordinatesOfPointsArray(&points);
    printCoordinatesOfAllPointsInArrays(&points);
}


POINT insertPointCoordinates()
{
    POINT pt;
    printf("Insert x and y coordinate of point: \n");
    scanf("%d %d", &pt.x, &pt.y);
    return pt;
}

void initializeCoordinatesOfPointsArray(POINT *points)
{
    for (int i=0; i<5; i++)
        points[i] = insertPointCoordinates();
}


void printCoordinatesOfAllPointsInArrays(POINT *points)
{
    for (int i=0; i<5; i++)
        printf("Point %d: (%d,%d)\n", i, points[i].x, points[i].y);
}
*/
