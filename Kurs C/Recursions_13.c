#include <stdio.h>
#include <stdlib.h>
/*
int getLucasSequenceValue(int num);

void main()
{
    int num = 7;
    printf("The Lucas Sequence value in index %d is %d", num, getLucasSequenceValue(num));
}

int getLucasSequenceValue(int num)
{
    if (num == 0)
        return 2;
    if (num == 1)
        return 1;
    if (num > 1)
        return getLucasSequenceValue(num-1) + getLucasSequenceValue(num-2);
}
*/
