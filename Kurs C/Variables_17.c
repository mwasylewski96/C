#include <stdio.h>
#include <math.h>
/*
int main()
{
    // Distance between 2 points (X1, Y1) and (X2, Y2)
    int X1, Y1, X2, Y2;
    float distance;
    printf("Insert X1 value:");
    scanf("%d", &X1);
    printf("Insert Y1 value:");
    scanf("%d", &Y1);
    printf("Insert X2 value:");
    scanf("%d", &X2);
    printf("Insert Y2 value:");
    scanf("%d", &Y2);
    distance = pow( (pow(X1-X2,2) + pow(Y1-Y2,2) ),0.5);
    printf("DISTANCE = %.2f", distance);
}
*/
