#include <stdio.h>
/*
int main()
{
    int max;
    printf("Insert a value to print the big table of multiplication by three: ");
    scanf("%d", &max);
    for (int i=1; i<=max; i++)
    {
        printf("%d. 3 * %d = %d\n", i, i, 3*i);
    }
    return 0;
}
*/
