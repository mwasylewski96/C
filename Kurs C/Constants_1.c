#include <stdlib.h>
#include <stdio.h>

int main()
{
    const age = 27; // default usage
    const int *ptr; // value cannot be changed but the pointer can
    int *const ptrr; // value can be changed but pointer cannot
    const int *const ptrrr // both of them cannot be changed
}
