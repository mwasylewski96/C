#include <stdlib.h>
#include <stdio.h>
/*
void printSequenceOfLowCharsAndHighChars(int total, char ch)
{
    if (total >= 1)
    {
        printf("%c", ch);
        printSequenceOfLowCharsAndHighChars(total-1, ch);
        printf("%c", ch-'a'+'A');
    }
}

void main()
{
    char ch = 'c';
    int total = 5;
    printSequenceOfLowCharsAndHighChars(total, ch);
}
*/
