#include <stdlib.h>
#include <stdio.h>
/*
int calculate_sum_of_divisors_of_given_number(int number)
{
    int sum_of_divisors = 0;
    for (int i=1; i<=number; i++)
    {
        if (number % i == 0)
            sum_of_divisors += i;
    }
    return sum_of_divisors;
}

int main()
{
    int num;
    printf("Insert a number to calculate its sum of divisors: ");
    scanf("%d", &num);
    int sum_of_divisors = calculate_sum_of_divisors_of_given_number(num);
    printf("Sum of divisors of given number: %d", sum_of_divisors);
    return 0;
}
*/
