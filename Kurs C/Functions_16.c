#include <stdlib.h>
#include <stdio.h>
/*
int optimise_divisor_func(int num)
{
    int sum = 1 + num;
    int i;
    for (i=2; i*i<num; i++)
    {
        if (num % i == 0)
            sum = sum + i + num/i;
    }
    if (i*i == num)
        sum += i;
    return sum;
}

int main()
{
    int num;
    printf("Insert a number to calculate its sum of divisors: ");
    scanf("%d", &num);
    int sum_of_divisors = optimise_divisor_func(num);
    printf("Sum of divisors of given number: %d", sum_of_divisors);
    return 0;
}
*/
