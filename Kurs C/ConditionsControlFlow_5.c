#include <stdio.h>
#include <stdio.h>
/*
int main()
{
    int a, b, c;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    if (a > b)
        if (a > c)
            if (b > c)
                printf("MAXIMUM = %d\nMINIMUM = %d\n", a, c);
            else
                printf("MAXIMUM = %d\nMINIMUM = %d\n", a, b);
        else
            printf("MAXIMUM = %d\nMINIMUM = %d\n", c, b);
    else
        if (a > c)
            printf("MAXIMUM = %d\nMINIMUM = %d", b, c);
        else
            if (c > b)
                printf("MAXIMUM = %d\nMINIMUM = %d", c, a);
            else
                printf("MAXIMUM = %d\nMINIMUM = %d", b, a);
}
*/
