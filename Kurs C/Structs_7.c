#include <stdlib.h>
#include <stdio.h>
/*
typedef struct point{
    int x;
    int y;
}Point;

typedef struct circle{
    Point center;
    double radius;
}Circle;

int main()
{
    Point p1 = {3, 4};
    Circle c1;
    c1.center = p1;
    c1.radius = 3.3;
    printf("radius = %.2f, center = (%d,%d)", c1.radius, c1.center.x, c1.center.y);
}
*/
