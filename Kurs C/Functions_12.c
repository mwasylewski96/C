#include <stdlib.h>
#include <stdio.h>
/*
int three_chars_to_integer(char a, char b, char c)
{
    int hundreths, tenths, oneths;
    if (a >= '0' && a <= '9')
        hundreths = a - '0';
    if (b >= '0' && b <= '9')
        tenths = b - '0';
    if (c >= '0' && c <= '9')
        oneths = c - '0';
    return hundreths*100+tenths*10+oneths;
}

int main()
{
    char a, b,c;
    printf("From chars get integer. Insert three chars: ");
    scanf("%c %c %c", &a, &b, &c);
    printf("%d", three_chars_to_integer(a,b,c));
}
*/
