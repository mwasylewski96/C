#include <stdlib.h>
#include <stdio.h>
/*
void max_from_two_digts(int number)
{
    int oneth, tenth;
    if (number>9 && number<100)
        {oneth = number % 10;
        tenth = number / 10;
        if (oneth >= tenth)
            printf("Oneth is higher = %d", oneth);
        else
            printf("Tenth is higher = %d", tenth);}
    else
        printf("Number is not two_digit number!");
}


int main()
{
    max_from_two_digts(304);
    return 0;
}
*/
