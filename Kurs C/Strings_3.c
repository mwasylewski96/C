#include <stdlib.h>
#include <string.h>
/*
int checkIsPalindrome(char *stringPtr);

int main()
{
    char stringuser[20];
    gets(stringuser);
    int result = checkIsPalindrome(stringuser);
    if (result == 0)
        printf("\nThis is Palindrome", result);
    else
        printf("\n%This is not Palindrome", result);
    return 0;
}

int checkIsPalindrome(char *stringPtr)
{
    int size = strlen(stringPtr);
    for (int i=0; i<=size; i++)
    {
        if (stringPtr[i] != stringPtr[size-1-i])
            return -1;
    }
    return 0;
}
*/
