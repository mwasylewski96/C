#include <stdio.h>
#define SIZE 6
/*
int main()
{
    int table[] = {2,1,5,6,8,9};
    printf("Before:\n");
    for (int i=0; i<SIZE; i++)
        printf("%d ", table[i]);
    int value_to_set_as_last = table[0];
    printf("\n");
    for (int i=1; i<SIZE; i++)
        table[i-1] = table[i];
    table[SIZE-1] = value_to_set_as_last;
    printf("After:\n");
    for (int i=0; i<SIZE; i++)
        printf("%d ", table[i]);
    return 0;
}
*/
