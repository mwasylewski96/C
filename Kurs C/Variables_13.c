#include <stdio.h>
/*
int main()
{
    int seconds, minutes, hours, remain_seconds;
    printf("Insert seconds: ");
    scanf("%d", &seconds);
    hours = seconds / 3600.0;
    minutes = (seconds % 3600) / 60.0;
    remain_seconds = (seconds % 3600) % 60;
    printf("%d:%d:%d\n", hours, minutes, remain_seconds);
    printf("Checking...\n");
    printf("%d = %d", seconds, hours*3600+minutes*60+remain_seconds);
}
*/
