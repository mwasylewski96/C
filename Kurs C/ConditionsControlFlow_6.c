#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
  int grade;
  printf("Insert a vaule of points in test: ");
  scanf("%d", &grade);
  if (grade > 80)
   printf("Excellent job!");
  else if (grade > 60)
   printf("Not bad.. you passed!");
  else
   printf("You failed the test!");

  return 0;
}
*/
