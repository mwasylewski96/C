#include <stdio.h>
/*
int main()
{
    float distance, speed, time;
    printf("Enter distance in miles: ");
    scanf("%f", &distance);
    printf("Enter speed miles per hour: ");
    scanf("%f", &speed);
    time = distance / speed;
    printf("time = distance / speed --> %f = %f / %f\n", time, distance, speed);
    printf("time = %.2fh", time);
}
*/
