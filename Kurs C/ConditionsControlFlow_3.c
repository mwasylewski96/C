#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int a;
    printf("Insert value to check is even or odd: ");
    scanf("%d", &a);
    if (a % 2 == 0)
        printf("Value %d is even", a);
    else
        printf("Value %d is odd", a);
}
*/
