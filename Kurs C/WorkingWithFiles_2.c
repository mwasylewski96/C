#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    char myCh1;
    fp = fopen("MyFile.txt", "r");

    if (fp != NULL)
    {
            printf("The file is Opened for Reading!\n");
            for (int i=0; i<6; i++)
            {
                myCh1 = fgetc(fp);
                printf("%c ", myCh1);
            }
        fclose(fp);
    }
}
*/
