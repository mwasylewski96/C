#include <stdlib.h>
/*
int main()
{
    int n_values, current_value;
    int odd_sum = 0;
    printf("Insert how many values to check odd digits and sum them: ");
    scanf("%d", &n_values);
    while (n_values > 0)
    {
        printf("Insert a value: ");
        scanf("%d", &current_value);
        if (current_value % 2 == 1)
            odd_sum += current_value;
        n_values --;
    }
    printf("Sum of all odd numbers = %d", odd_sum);
    return 0;
}
*/
