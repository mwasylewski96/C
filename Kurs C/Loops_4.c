#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int grade;
    do
    {
        printf("Insert a grade between < 0, 100 >");
        scanf("%d", &grade);
        if (grade >= 0 && grade <= 100)
            printf("Thanks! It is a valid grade!");
    } while (grade >= 0 && grade <= 100);
    printf("NO! it is not a valid grade. I am ending programm");
    return 0;
}
*/
