#include <stdlib.h>
#define SIZE 9
/*
int main()
{
    int table[] = {23,23,2,6,2,6,77,7,7};
    int number_of_unique_elements = 0;
    int counter_uniq = 0;
    int unique_value;
    for (int i=0; i<SIZE; i++)
    {
        unique_value = 1;
        for (int j=0; j<SIZE; j++)
            if (table[i] == table[j] && i!=j)
                {
                    unique_value = 0;
                    break;
                }
        if (unique_value == 1)
        {
            printf("Uniq value is %d\n", table[i]);
            counter_uniq ++;
        }
    }
    printf("All unique values is %d", counter_uniq);
}
*/
