#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int grade = 0;
    int sum_of_grades = 0;
    int number_of_grades = 0;
    printf("Enter all grades to calculate average grade: \n");
    printf("(If you enter all grades insert -1)\n");
    while (grade != -1)
    {
        scanf("%d", &grade);
        if (grade != -1)
            {number_of_grades ++; sum_of_grades += grade;}
        else
            printf("The end of inserting grades!\n");

    }
    printf("The average value is %f", (float)sum_of_grades/number_of_grades);
    return 0;
}
*/
