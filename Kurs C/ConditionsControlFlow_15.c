#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int a, b, c, min, max, value_2;
    printf("Divisible or Non-Divisible? insert three digits: ");
    scanf("%d %d %d", &a, &b, &c);
    if (a == 0 || b == 0 || c == 0)
        printf("Not insert 0");
    else if ((a % b == 0 || b % a == 0) && (a % c == 0 || c % a == 0) && (c % b == 0 || b % c == 0))
        printf("Divisible");
    else
        printf("Non-Divisible");
    return 0;
}
*/
