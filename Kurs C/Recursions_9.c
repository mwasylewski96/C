#include <stdlib.h>
#include <stdio.h>
/*
int getTotalAmountOfEvenNumbers();

void main()
{
    printf("Total even numbers in given sequence is %d", getTotalAmountOfEvenNumbers());
}

int getTotalAmountOfEvenNumbers()
{
    int value;
    scanf("%d", &value);
    if (value<=-1)
        return 0;
    if (value % 2 == 0)
        return 1 + getTotalAmountOfEvenNumbers();
    return getTotalAmountOfEvenNumbers();
}
*/
