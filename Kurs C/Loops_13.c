#include <stdio.h>
/*
int main()
{
    int num;
    int temp_value = 0;
    printf("How many even numbers from 2 to print?");
    scanf("%d", &num);
    for (int i=1; i<=num; i++)
    {
        temp_value = temp_value + 2;
        if (temp_value == num*2)
            printf("%d", temp_value);
        else
            printf("%d, ", temp_value);
    }
    return 0;
}
*/
