#include <stdio.h>
/*
int main()
{
    // SUM of ARITHMETIC SEQUENCE
    float sum_n, a1, an;
    int n;
    printf("Enter initial term: ");
    scanf("%f", &a1);
    printf("Enter n-th element: ");
    scanf("%f", &an);
    printf("Enter total terms: ");
    scanf("%d", &n);
    sum_n = (a1+an)/2 * n;
    printf("Sum sum_n = (a1+an)/2 * n\n");
    printf("Sum %f = (%f+%f)/2 * %d\n", sum_n, a1, an, n);
}
*/
