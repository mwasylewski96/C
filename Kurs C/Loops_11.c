#include <stdio.h>
/*
int main()
{
    int num;
    int sum = 0;
    printf("Insert a max value to print a sum of all following number: ");
    scanf("%d", &num);
    for (int i=1; i<=num; i++)
    {
        sum = sum + i;
    }
    printf("Sum of: ");
    for (int i=1; i<=num; i++)
    {
        printf("%d ", i);
        if (i != num)
            printf("+ ");
    }
    printf("= %d", sum);
}
*/
