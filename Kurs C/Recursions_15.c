#include <stdlib.h>
#include <stdio.h>
/*
int getPellNumberValue(int index);


int main()
{
    int index = 5;
    if (index >=0)
        printf("Value on index %d in PellSequence is %d", index, getPellNumberValue(index));
    else
        printf("Index must be non negative integer!");
}


int getPellNumberValue(int n)
{
    if (n == 0)
        return 0;
    if (n == 1)
        return 1;
    if (n > 1)
        return 2*getPellNumberValue(n-1) + getPellNumberValue(n-2);
}
*/
