#include <stdlib.h>
#include <stdio.h>
/*
void collect_all_numbers_to_tab(int tab[], int size)
{
    for (int i=0; i<size; i++)
    {
        scanf("%d", &tab[i]);
    }
}

int find_max_index_in_telephone_number(int tab[], int size)
{
    int max_index;
    int max_digit = 0;
    for (int i=0; i<size; i++)
    {
        if (tab[i]>= max_digit)
            {
                max_digit = tab[i];
                max_index = i;
            }
    }
    return max_index;
}

int main()
{
    int size = 9;
    int tablica[size];
    printf("Insert telephone number: \n");
    collect_all_numbers_to_tab(tablica, size);
    int max_index = find_max_index_in_telephone_number(tablica, size);
    printf("Max digit is in index %d", max_index);
    return 0;
}
*/
