#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    fp = fopen("MyFile.txt", "r");
    if (fp != NULL)
    {
        int counter = 0;
        while (!feof(fp))
            {
                printf("%c" ,fgetc(fp));
                counter++;
            }
        printf("Number of chars is %d", counter);
        fclose(fp);
    }

}
*/
