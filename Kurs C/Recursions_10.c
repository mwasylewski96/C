#include <stdlib.h>
#include <stdio.h>
/*
int getTotalSumOfEvenNumbers(int sum);

void main()
{
    int sum = 0;
    printf("Total sum of even numbers in given sequence is %d", getTotalSumOfEvenNumbers(sum));
}

int getTotalSumOfEvenNumbers(int sum)
{
    int value;
    scanf("%d", &value);
    if (value<=-1)
        return sum;
    if (value % 2 == 0)
        {
            sum += value;
            return getTotalSumOfEvenNumbers(sum);
        }
    return getTotalSumOfEvenNumbers(sum);
}
*/
