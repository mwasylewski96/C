#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    fp = fopen("MyFile2.txt", "w");
    if (fp != NULL)
    {
        for (int i=1; i<=10; i++)
            fprintf(fp, "%d %d\n", i, i*i);
        fclose(fp);
    }

}
*/
