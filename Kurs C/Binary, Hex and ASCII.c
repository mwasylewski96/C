#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int day, month, year;
    printf("Insert some date DD-MM-YYYY:");
    scanf("%d %d %d", &day, &month, &year);
    printf("Date in hexadecimal format: 0x%X-0x%X-0x%X", day, month, year);
}
*/
