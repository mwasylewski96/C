#include <stdlib.h>
#include <stdio.h>
/*
char toLower(char letter)
{
    if (letter >= 'A' && letter <= 'Z')
        return letter - 'A' + 'a';
    else if (letter >= 'a' && letter <= 'z')
        return letter;
    else
        return '0';
}

int main()
{
    char lower_letter;
    lower_letter = toLower('1');
    printf("lower letter %c", lower_letter);
}
*/
