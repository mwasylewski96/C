#include <stdlib.h>
#include <stdio.h>
/*
int getTotalAmountOfIntegersSmallerThanNum(int num);

void main()
{
    int num;
    printf("Insert value to check lower values: ");
    scanf("%d", &num);
    printf("Total numbers lower than %d is %d", num, getTotalAmountOfIntegersSmallerThanNum(num));
}

int getTotalAmountOfIntegersSmallerThanNum(int num)
{
    int a;
    scanf("%d", &a);
    if (a<=-1)
        return 0;
    if (a<=num)
        return 1 + getTotalAmountOfIntegersSmallerThanNum(num);
    return getTotalAmountOfIntegersSmallerThanNum(num);
}
*/
