#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int seconds, minutes, hours;
    printf("Print time from given seconds: ");
    scanf("%d", &seconds);
    hours = seconds / 3600;
    minutes = (seconds % 3600) / 60;
    seconds = (seconds % 3600) % 60;
    if (hours < 10)
        printf("0");
    printf("%d:", hours);
    if (minutes < 10)
        printf("0");
    printf("%d:", minutes);
    if (seconds < 10)
        printf("0");
    printf("%d", seconds);
    return 0;
}
*/
