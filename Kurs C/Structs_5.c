#include <stdlib.h>
#include <stdio.h>
/*
typedef struct point{
    int x;
    int y;
}POINT;

int compareTwoPoints(POINT pt1, POINT pt2)
{
    if ((pt1.x != pt2.x) || (pt1.y != pt2.y))
        return 0;
    return 1;
}
void main()
{
    POINT pt1 = {2,3}, pt2 = {2,3};
    int result = compareTwoPoints(pt1, pt2);
    printf("Result of comparision two points is %d", result);
}
*/
