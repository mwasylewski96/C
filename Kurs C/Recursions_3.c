#include <stdlib.h>
/*
int factorialUpTo(int num);

void main()
{
    int num = 4;
    printf("Factorial 1 to %d is %d", num, factorialUpTo(num));
}

int factorialUpTo(int num)
{
    if (num<=1)
        return 1;
    return num * factorialUpTo(num-1);
}
*/
