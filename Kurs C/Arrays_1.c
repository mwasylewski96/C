#include <stdlib.h>
#include <stdio.h>
/*
void collect_all_numbers_to_tab(int tab[], int size)
{
    for (int i=0; i<size; i++)
    {
        scanf("%d", &tab[i]);
    }
}

int find_max_digit_in_telephone_number(int tab[], int size)
{
    int max_digit = 0;
    for (int i=0; i<size; i++)
    {
        printf("%d ", tab[i]);
        if (tab[i]>= max_digit)
            max_digit = tab[i];
    }
    return max_digit;
}

int main()
{
    int size = 9;
    int tablica[size];
    printf("Insert telephone number: \n");
    collect_all_numbers_to_tab(tablica, size);
    int max_digit = find_max_digit_in_telephone_number(tablica, size);
    printf("\nMax digit is %d", max_digit);
    return 0;
}
*/
