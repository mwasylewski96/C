#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int base, power;
    int value = 1;
    char charpower;
    printf("Write a calculation to power some value: ");
    scanf("%d %c %d", &base, &charpower, &power);
    int counter = power;
    if ( (charpower != '^' ) || ( base < 0 ) || ( power < 0 ) )
        printf("Something go wrong. Check base, charpower and power again");
    else
    {
        while (counter>0)
        {
            value = value * base;
            counter--;

        }
        printf("%d%c%d=%d",base, charpower, power, value);
    }
}
*/
