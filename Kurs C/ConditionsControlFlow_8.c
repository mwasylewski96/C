#include <stdio.h>
#include <stdlib.h>
// Ternary operations
/*
int main()
{
    int a, b, min, max;
    printf("Insert a: ");
    scanf("%d", &a);
    printf("Insert b: ");
    scanf("%d", &b);
    min = (a > b)? b: a;
    max = (a > b)? a: b;
    printf("min = %d, max = %d", min, max);
    return 0;
}
*/
