#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    float start_one_cent = 0.01;
    for (int i=1; i<=30; i++)
    {
        start_one_cent = start_one_cent + start_one_cent;
        if (start_one_cent> 1000000)
            {
                printf("So the better option is doubling 0.01 because after %d days we have %.2f$\n", i, start_one_cent);
                break;
            }
        else if (i==30 && start_one_cent < 1000000)
        {
            printf("Value of doubling the initial value per 30 days: %.2f$\n", start_one_cent);
            printf("One milion dollar: %d$\n", 1000000);
            printf("So the better option is getting now 1 milion dollar");
        }
    }
    return 0;
}
*/
