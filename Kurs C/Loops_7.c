#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int base_of_tree;
    int temp_base=1;
    printf("Print a tree with digits. Insert a value:\n");
    scanf("%d", &base_of_tree);
    printf("\n");
    while (temp_base<=base_of_tree)
    {
        for (int i = 1; i <= temp_base; i++)
        {
            printf("%d", i);
        }
        printf("\n");
        temp_base++;
    }
    return 0;
}
*/
