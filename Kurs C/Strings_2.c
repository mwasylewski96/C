#include <stdlib.h>
#include <stdio.h>
/*
int calculateWordsInString(char *strPtr);

int main()
{
    char stringtext[100];
    gets(stringtext);
    int length = calculateWordsInString(stringtext);
    printf("%d", length);
}



int calculateWordsInString(char *strPtr)
{
    int i = 0;
    int length = 0;
    while (*(strPtr+i) != '\0')
    {
        if (*(strPtr+i) == ' ')
            length++;
        i++;
    }
    length++;
    return length;
}
*/
