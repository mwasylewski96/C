#include <stdlib.h>
/*
int calculateLengthOfGivenString(char *strPtr);

int main()
{
    char readstring[10];
    scanf("%s", readstring);
    int length = calculateLengthOfGivenString(readstring);
    printf("Read string length is %d", length);
}

int calculateLengthOfGivenString(char *strPtr)
{
    int length = 0;
    while (*(strPtr+length) != '\0')
        length ++;
    return length;
}
*/
