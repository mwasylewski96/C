#include <stdlib.h>
#define SIZE 10
/*
int main()
{
    int tab[SIZE];
    for (int i=0; i<SIZE; i++)
    {
        scanf("%d", &tab[i]);
    }
    int type = 0;
    for (int i=1; i<SIZE; i++)
    {
        if (tab[i-1]==tab[i])
            type = 1;
        if (tab[i-1]>tab[i])
            {
                type = 2;
                break;
            }
    }
    switch (type)
    {
        case 0:
            printf("Really sorted");
            break;
        case 1:
            printf("Sorted");
            break;
        case 2:
            printf("Not sorted");
            break;
    }

}
*/
