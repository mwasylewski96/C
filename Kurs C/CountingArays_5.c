#include <stdlib.h>
/*
#define SIZE 8
#define MAPVAL 'a'
#define amount_of_letters 'z' - 'a' + 1

void main()
{
    int countArr[amount_of_letters] = {0};
    char arr[SIZE] = {'k','i','b','r','c','k','z','m'};

    for (int i=0; i<SIZE; i++)
    {
        int index = arr[i]-MAPVAL;
        countArr[index]++;
    }

    for (int i=0; i<SIZE; i++)
    {
        printf("%c ", arr[i]);
    }

    printf("\n");

    for (int i=0; i<amount_of_letters; i++)
    {
        printf("%d ", countArr[i]);
    }
    int max = 0;
    int amount = 0;
    for (int i=0;i<amount_of_letters;i++)
    {
        if (countArr[i] > amount)
        {
            max = i;
            amount = countArr[i];
        }
    }
    printf("\nValue '%c' appears most time: %d", max+MAPVAL, amount);

}

*/
