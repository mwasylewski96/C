#include <stdlib.h>
#include <stdio.h>
/*
typedef struct point{
    int x;
    int y;
}POINT;

POINT insertPoint()
{
    POINT pt;
    scanf("%d %d", &pt.x, &pt.y);
    return pt;
}

void printPoint(POINT pt)
{
    printf("The point is (%d,%d)", pt.x, pt.y);
}

void main()
{
    POINT pt1 = insertPoint();
    printPoint(pt1);
}
*/
