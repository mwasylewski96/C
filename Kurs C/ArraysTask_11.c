#include <stdio.h>
/*
void ResetAllElements(int *sometable, int size);
void PrintAllResetElements(int *reset_table, int size);

int main()
{
    int arr[7] = {1,3,4,7,8,9,60};
    ResetAllElements(arr, 7);
    PrintAllResetElements(arr, 7);
    return 0;
}

void ResetAllElements(int *sometable, int size)
{
    for (int i=0; i<size; i++)
        *(sometable+i) = 0;
}

void PrintAllResetElements(int *reset_table, int size)
{
    for (int i=0; i<size; i++)
        printf("%d ", *(reset_table+i));
}
*/
