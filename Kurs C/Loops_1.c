#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int counter;
    printf("How many stars you want to see on the screen? Insert value: ");
    scanf("%d", &counter);
    while (counter > 0)
    {
        if (counter == 1)
            printf("*");
        else
            printf("*\n");
        counter = counter - 1;
    }
    return 0;
}
*/
