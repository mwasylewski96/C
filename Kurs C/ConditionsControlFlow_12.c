#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    float x,y;
    printf("Checking quadrant o (x, y) Point : ");
    scanf("%f %f", &x, &y);
    if (x == 0 || y == 0)
        printf("Not possible to say which Quadrant");
    else
        if (x > 0 && y > 0)
            printf("Point is in Quadrant 1");
        else if (x > 0 && y < 0)
            printf("Point is in Quadrant 4");
        else if (x < 0 && y < 0)
            printf("Point is in Quadrant 3");
        else
            printf("Point is in Quadrant 2");
    return 0;
}
*/
