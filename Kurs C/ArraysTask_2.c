#include <stdlib.h>
#define SIZE 8
/*
int checkPalindrome(int tab[])
{
    for (int i=0; i<SIZE/2; i++)
{
    if (tab[i] != tab[SIZE-1-i])
        return 0;
}
    return 1;
}

void main()
{
    int table[] = {1,5,2,30,30,2,5,1};
    int tf = checkPalindrome(table);
    if (tf == 0)
        printf("It is not palindrome");
    else
        printf("It is palindrome");
}
*/
