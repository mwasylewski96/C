#include <stdio.h>
/*
int main()
{
    float month_salary, hour_salary;
    int hours;
    printf("Enter salary per hour: ");
    scanf("%f", &hour_salary);
    printf("Enter hours in month: ");
    scanf("%d", &hours);
    month_salary = hour_salary * hours;
    printf("Month salary -> %f * %d = %f", hour_salary, hours, month_salary);
}
*/
