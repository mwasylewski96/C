#include <stdlib.h>
/*
int sumUpTo(int num);

void main()
{
    int num = 5;
    printf("Sum from %d to 1 is %d", num, sumUpTo(num));
}

int sumUpTo(int num)
{
    if (num==1)
        return 1;
    return num + sumUpTo(num-1);
}
*/
