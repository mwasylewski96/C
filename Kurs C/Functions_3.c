#include <stdlib.h>
#include <stdio.h>
/*
void max3between()
{
    int a, b, c;
    printf("Insert a, b, c: ");
    scanf("%d %d %d", &a, &b, &c);
    if (a>=b&&a>=c)
        printf("Max is a = %d", a);
    if (b>=a&&b>=c)
        printf("Max is b = %d", b);
    if (c>=a&&c>=b)
        printf("Max is c = %d", c);
}

int main()
{
    max3between();
    return 0;
}
*/
