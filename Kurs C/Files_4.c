#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    int table[2];
    fp = fopen("MyFile2.txt", "r");
    if (fp != NULL)
    {
        for (int i=1; i<=10; i++)
        {
            fscanf(fp, "%d %d\n", &table[0], &table[1]);
            printf("%d %d\n", table[0], table[1]);
        }
        fclose(fp);
    }

}
*/
