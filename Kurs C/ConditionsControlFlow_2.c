#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    if (a > b)
        printf("Grater value %d", a);
    else if (a == b)
       printf("Both values are the same %d = %d", a, b);
    else
        printf("Greater value %d", b);
}
*/
