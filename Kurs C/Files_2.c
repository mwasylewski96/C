#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    fp = fopen("MyFile.txt", "r");
    char chr;
    if (fp != NULL)
    {
        int counter = 1;
        while (!feof(fp))
            {
                chr = fgetc(fp);
                if (chr == '\n')
                    counter++;
            }
        printf("Number of rows is %d", counter);
        fclose(fp);
    }

}
*/
