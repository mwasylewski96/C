#include <stdio.h>
#define SIZE 6
#define N 1
/*
int main()
{
    int table[] = {2,1,5,6,8,9};
    printf("Before:\n");
    for (int i=0; i<SIZE; i++)
        printf("%d ", table[i]);
    for (int repeat=0; repeat<N; repeat++)
    {
    int value_to_set_as_last = table[SIZE-1];
    for (int i=SIZE-1; i>0; i--)
        table[i] = table[i-1];
    table[0] = value_to_set_as_last;
    }
    printf("\n");
    printf("After:\n");
    for (int i=0; i<SIZE; i++)
        printf("%d ", table[i]);
    return 0;
}
*/
