#include <stdio.h>
/*
int SumOfArray(int *array, int size);

int main()
{
    int table[5] = {1,2,30,4,-37};
    int result = SumOfArray(table, 5);
    printf("Result is %d", result);
}


int SumOfArray(int *array, int size)
{
    int sum = 0;
    for (int i=0; i<size; i++)
    {
        // sum += array[i];
        // or this below
        sum += *(array+i);
        printf("sum temp is %d\n", sum);
    }
    return sum;
}
*/
