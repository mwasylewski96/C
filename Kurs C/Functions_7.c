#include <stdlib.h>
#include <stdio.h>
/*
long int calculate_factorial(int num)
{
    long int sum = 1;
    for (int i=1; i<=num; i++)
    {
        sum *= i;
    }
    return sum;
}

int main()
{
    int num;
    printf("Calculate factorial. Insert n number value:");
    scanf("%d", &num);
    long int sum;
    sum = calculate_factorial(num);
    printf("Factorial = %li", sum);
    return 0;
}
*/
