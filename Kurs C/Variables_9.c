#include <stdio.h>
/*
int main()
{
    // ARITHMETHIC SEQUENCE TASK 1
    // an = a1 + (n - 1) * d
    // d = 2, a1 = 1, n = 9, an = ?
    float an, a1, d;
    int n;
    printf("Enter difference value: ");
    scanf("%f", &d);
    printf("Enter initial term: ");
    scanf("%f", &a1);
    printf("Enter which term: ");
    scanf("%d", &n);
    an = a1 + (n - 1) * d;
    printf("an = a1 + (n - 1) * d\n");
    printf("%f = %f + (%d - 1) * %f\n", an, a1, n, d);
}
*/
