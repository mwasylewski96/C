#include <stdlib.h>
#include <stdio.h>
/*
float get_average_value_of_digits_in_given_number_less_than_given_digit(int num, int digit)
{
    int rest_from_num;
    int sum_of_less_digits = 0;
    int number_of_less_digits = 0;
    while(num != 0)
    {
        rest_from_num = num % 10;
        if (rest_from_num < digit)
        {
            sum_of_less_digits += rest_from_num;
            number_of_less_digits ++;
        }
        num /= 10;
    }
    printf("Number of digits less than digit = %d\n", number_of_less_digits);
    return (float)sum_of_less_digits/number_of_less_digits;
}

int main()
{
    int num, digit;
    printf("Insert value of number and digit:");
    scanf("%d %d", &num, &digit);
    float avg_value = get_average_value_of_digits_in_given_number_less_than_given_digit(num, digit);
    printf("Average value of digits in given number less than given digit: %f", avg_value);
    return 0;
}
*/
