#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int number, power;
    char charpower;
    printf("Insert a calculation to power some value (use ^):\n");
    scanf("%d %c %d", &number, &charpower, &power);
    int temp_value = 1;
    if (charpower == '^' && number > 0 && power > 0)
        {
            for (int i = 1; i <= power; i++)
            {
            temp_value = temp_value * number;
            }
        printf("%d%c%d=%d", number, charpower, power, temp_value);
        }
    else
        printf("Check inserted values again! char should be `^` and number >0 and power >0");
    return 0;
}
*/
