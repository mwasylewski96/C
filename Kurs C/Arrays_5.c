#include <stdlib.h>
#include <stdio.h>
#define SIZE 10
/*
void print_in_reverse_pullups(int tab[])
{
    for (int i=SIZE-1; i>=0; i--)
    {
        printf("Number of pullups is %d\n", tab[i]);
    }
}

void collect_all_numbers(int tab[])
{
    for (int i=0; i<SIZE; i++)
    {
        printf("Insert a number of pullups: ");
        scanf("%d", &tab[i]);
    }
}

void main()
{
    int tab[SIZE];
    collect_all_numbers(tab);
    print_in_reverse_pullups(tab);
}
*/
