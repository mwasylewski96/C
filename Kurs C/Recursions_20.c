#include <stdlib.h>
#include <stdio.h>
/*
void printSequenceOfChar1sAndChar2s(int total, char ch1, char ch2)
{
    if (total >= 1)
    {
        printf("%c", ch1);
        printSequenceOfChar1sAndChar2s(total-1, ch1, ch2);
        printf("%c%c", ch2, ch2);
    }
}

void main()
{
    char ch1 = 'c', ch2 = 'd';
    int total = 2;
    printSequenceOfChar1sAndChar2s(total, ch1, ch2);
}
*/
