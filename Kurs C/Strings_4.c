#include <stdlib.h>
/*
void strcpy(char *destination, char *source);

int main()
{
    char name[] = {"ProgrammerMarcinek"};
    char cpyname[30];
    strcpy(cpyname, name);
    printf("source is '%s' and destination is '%s' ", name, cpyname);
}

void strcpy(char *destination, char *source)
{
    int i = 0;
    while (source[i] != '\0')
        {destination[i] = source[i];
        i++;}
    destination[i] = '\0';
}
*/
