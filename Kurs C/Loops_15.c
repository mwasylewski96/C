#include <stdio.h>
/*
int main()
{
    // sum of number which can be divided by three and by five
    // so we can check divided by fiftheen only
    int num;
    printf("Insert a max number to checking values divided by fiftheen\n");
    scanf("%d", &num);
    int sum = 0;
    for (int i=15; i<=num; i+=15)
    {
        if (i%15 == 0)
            sum += i;
    }
    printf("Sum of this values = %d", sum);
}
*/
