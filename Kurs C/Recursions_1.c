#include <stdlib.h>
/*
void sum_all_lower_integers(int *ptr, int num)
    {
        *ptr += num;
        if (num < 1)
            printf("Sum is %d", *ptr);
        else
        {
            num --;
            sum_all_lower_integers(ptr, num);
        }
    }

int main()
{
    int sum = 0, num = 4;
    sum_all_lower_integers(&sum, num);
}
*/
