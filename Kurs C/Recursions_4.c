#include <stdlib.h>
/*
int getFibonacciValue(int num)
{
    switch (num)
    {
        case 0:
            return 0;
        case 1:
            return 1;
        default:
            return getFibonacciValue(num-1)+ getFibonacciValue(num-2);
    }
}

int main()
{
    int num = 10;
    for (int i=num; i>-1; i--)
        printf("Fibonacci value on index %d is %d\n", i, getFibonacciValue(i));
}
*/
