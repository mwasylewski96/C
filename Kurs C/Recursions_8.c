#include <stdlib.h>
#include <stdio.h>
/*
int getTotalAmountOfCharsEqualsToChar(char a);

void main()
{
    char a;
    printf("Insert char to check total amount of equals char: ");
    scanf("%c", &a);
    printf("Total amount of chars equals to given char '%c' is %d", a, getTotalAmountOfCharsEqualsToChar(a));
}

int getTotalAmountOfCharsEqualsToChar(char a)
{
    char b;
    scanf("%c", &b);
    if (b =='$')
        return 0;
    if (b==a)
        return 1 + getTotalAmountOfCharsEqualsToChar(a);
    return getTotalAmountOfCharsEqualsToChar(a);
}
*/
