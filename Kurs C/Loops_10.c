#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int max;
    printf("Insert max value and I will print from 1 to your value and vice versa\n");
    scanf("%d", &max);
    for (int i=1; i<=max; i++)
    {
        printf("%d", i);
        if (i != max)
            printf(",");
    }
    printf("\n");
    for (int i=max; i>=1; i--)
    {
        printf("%d", i);
        if (i != 1)
            printf(",");
    }
}
*/
