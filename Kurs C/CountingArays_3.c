#include <stdlib.h>
/*
#define SIZE 20

void main()
{
    int arr[SIZE] = {5,4,3,1,2,1,5,8,6,7,5,7,5,9,9,8,8,8,8,8};
    int countArr[10] = {0};

    for (int i=0; i<SIZE; i++)
    {
        countArr[arr[i]]++;
    }

    for (int i=0; i<SIZE; i++)
    {
        printf("%d ", arr[i]);
    }

    printf("\n");

    for (int i=0; i<10; i++)
    {
        printf("%d ", countArr[i]);
    }
    int max = 0;
    int amount = 0;
    for (int i=0;i<10;i++)
    {
        if (countArr[i] > amount)
        {
            max = i;
            amount = countArr[i];
        }
    }
    printf("\nValue %d appears most time: %d", max, amount);

}
*/
