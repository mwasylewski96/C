#include <stdio.h>
#include <stdlib.h>
/*
void MinMax(int a, int b)
{
    int min, max;
    if (a >= b)
        {max = a;
        min = b;}
    else
        {max = b;
        min = a;}
    printf("Max = %d, Min = %d", max, min);
}

int main()
{
    MinMax(30,5);
    return 0;
}
*/
