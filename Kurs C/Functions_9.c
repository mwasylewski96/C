#include <stdlib.h>
#include <stdio.h>
/*
long long int nines_func(int num)
{
    long long int number = 0;
    if (num != 0)
        for (int i=1;i<=num; i++)
        {
            number = number*10 + 9;
        }
    return number;
}

int main()
{
    int length;
    long long int number;
    printf("Insert length of nine number: ");
    scanf("%d", &length);
    number = nines_func(length);
    printf("The number is %lld", number);
    return 0;
}
*/
