#include <stdio.h>
#include <math.h>
/*
int main()
{
    int number, sum_of_digits;
    int hundreths, tenths, oneths;
    printf("Insert number with three digits: ");
    scanf("%d", &number);
    hundreths = number / 100;
    printf("%d hundreths\n", hundreths);
    tenths = (number-hundreths*100) / 10;
    oneths = number - 100*hundreths - 10*tenths;
    sum_of_digits = hundreths + tenths + oneths;
    printf("sum_of_digits = hundreths + tenths + oneths\n", sum_of_digits, hundreths, tenths, oneths);
    printf("%d = %d + %d + %d", sum_of_digits, hundreths, tenths, oneths);
}
*/
