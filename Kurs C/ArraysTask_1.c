#include <stdlib.h>
/*
void main()
{
    int calendar[] = {20,11,2023};
    int calendar2[3];
    int *ptr;
    ptr = &calendar2;

    for (int i=0; i<=2; i++)
        *(ptr+i) = calendar[i];

    printf("calendar %d,%d,%d\n", calendar[0], calendar[1], calendar[2]);
    printf("calendar2 %d,%d,%d\n", calendar2[0], calendar2[1], calendar2[2]);
}
*/
