#include <stdlib.h>
#include <stdio.h>
/*
int from_one_to_nine(int length)
{
    int num = 0;
    for (int i=0; i<=length; i++)
    {
        num = num*10 + i;
    }
    return num;
}

long long int nines(int length)
{
    long long int num = 999999999;
    for (int i=10; i<=length; i++)
    {
        num = num*10 + 9;
    }
    return num;
}

int main()
{
    int length;
    printf("Insert value of length: ");
    scanf("%d", &length);
    if (length <=9)
        printf("%lld", from_one_to_nine(length));
    else
        printf("%lld", nines(length));
    return 0;
}
*/
