#include <stdlib.h>
#include <stdio.h>
/*
void main()
{
    FILE* fp;
    fp = fopen("MyFile.txt", "r");

    if (fp == NULL)
        printf("The opening of the file was Failed");
    else
    {
        printf("The file is Opened for Reading!");
        fclose(fp);
    }
}
*/
