#include <stdlib.h>
#include <stdio.h>
/*
int max3between(int a, int b, int c)
{
    if (a>=b&&a>=c)
        return a;
    if (b>=a&&b>=c)
        return b;
    if (c>=a&&c>=b)
        return c;
}

int main()
{
    int max_value;
    max_value = max3between(122, 1110, 29);
    printf("Max value is %d", max_value);
    return 0;
}
*/
