#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    char one_char;
    printf("Insert one character to check its type: ");
    scanf("%c", &one_char);
    if (one_char >= 0x30 && one_char <=0x39)
        printf("It is a digit!");
    else if (one_char >= 0x41 && one_char <=0x5A)
        printf("It is a Uppercase letter");
    else if (one_char >= 0x61 && one_char <=0x7A)
        printf("It is a Lowercase letter");
    else
        printf("It is another char");
}
*/
