#include <stdio.h>
#include <stdlib.h>

/*
int main()
{
    int number;
    printf("Insert number to check is double-digit or triple-digit or other: ");
    scanf("%d", &number);
    if (number > 9 && number <=99)
        printf("Double-digit number");
    else if (number > 99 && number <= 999)
        printf("Triple-digit number");
    else
        printf("Neither double/triple digit");
}
*/
