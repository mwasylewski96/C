#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
/*
bool isEven(int num)
{
    if (num % 2 == 0)
        return true;
    return false;
}

int main()
{
    int num;
    bool bool_value;
    printf("Insert value to check is even or odd: ");
    scanf("%d", &num);
    bool_value = isEven(num);
    printf("%s", bool_value?"True":"False");
    return 0;
}
*/
