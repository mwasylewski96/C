#include <stdlib.h>
#include <stdio.h>
/*
int findMax(int num, int max_val);

void main()
{
    int num = 10;
    int max_val = num;
    printf("Max value from given sequence is %d", findMax(num, max_val));
}

int findMax(int num, int max_val)
{
    int temp;
    if (num <= -1)
        return max_val;
    if (max_val < num)
        max_val = num;
    scanf("%d", &temp);
    findMax(temp, max_val);
}
*/
